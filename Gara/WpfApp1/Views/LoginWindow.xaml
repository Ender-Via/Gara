<Window x:Class="WpfApp1.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Đăng nhập hệ thống Gara" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource SidebarBgColor}"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">

    <Border CornerRadius="20" Background="{StaticResource SidebarBgColor}" BorderBrush="{StaticResource PrimaryColor}" BorderThickness="1">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,30">
                <materialDesign:PackIcon Kind="Garage" Foreground="{StaticResource PrimaryColor}" Width="60" Height="60" HorizontalAlignment="Center"/>
                <TextBlock Text="ĐĂNG NHẬP" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <TextBlock Text="Quản lý Gara Ô tô" FontSize="14" Foreground="#757681" HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Form -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <materialDesign:Card Padding="15" Background="#252D3A" UniformCornerRadius="10">
                    <StackPanel>
                        <TextBox x:Name="txtEmail"
                                 materialDesign:HintAssist.Hint="Email"
                                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                 Foreground="White"
                                 CaretBrush="White"
                                 Margin="0,0,0,15"/>

                        <PasswordBox x:Name="txtPassword"
                                     materialDesign:HintAssist.Hint="Mật khẩu"
                                     Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                                     Foreground="White"
                                     CaretBrush="White"/>
                    </StackPanel>
                </materialDesign:Card>

                <TextBlock x:Name="lblError" Text="" Foreground="#EF4444" FontSize="12" Margin="0,10,0,0" HorizontalAlignment="Center" Visibility="Collapsed"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Row="3">
                <Button x:Name="btnLogin"
                        Content="ĐĂNG NHẬP"
                        Height="45"
                        Background="{StaticResource PrimaryColor}"
                        BorderBrush="{StaticResource PrimaryColor}"
                        Click="btnLogin_Click"
                        materialDesign:ButtonAssist.CornerRadius="10"/>

                <Button Content="THOÁT"
                        Height="45"
                        Margin="0,10,0,0"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Foreground="#757681"
                        Click="btnExit_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
