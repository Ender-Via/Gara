<Window x:Class="WpfApp1.PhieuSuaChuaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Phiếu sửa chữa" Height="800" Width="1280" WindowStartupLocation="CenterScreen"
        Background="#FAF8FF" FontFamily="Inter">

    <Window.Resources>
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E1E2ED"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E1E2ED"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F3F3FE"/>
            <Setter Property="Foreground" Value="#434655"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,12"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E1E2ED"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <local:SidebarControl Grid.Column="0" ActiveItem="BM2"/>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="64"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" BorderBrush="#E1E2ED" BorderThickness="0,0,0,1" Padding="24,0">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="PHIẾU SỬA CHỮA" FontSize="18" FontWeight="Bold" Foreground="#191B23" VerticalAlignment="Center"/>
                        <Border Width="1" Height="24" Background="#E1E2ED" Margin="16,0"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="32" MaxWidth="1200" HorizontalAlignment="Stretch">

                    <Border Background="White" BorderBrush="#E1E2ED" BorderThickness="1" CornerRadius="8" Padding="24" Margin="0,0,0,24">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                <materialDesign:PackIcon Kind="Info" Foreground="#2563EB" Width="20" Height="20" VerticalAlignment="Center"/>
                                <TextBlock Text="Thông tin chung" FontSize="14" FontWeight="SemiBold" Foreground="#191B23" Margin="8,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>

                            <UniformGrid Columns="2" Rows="1">
                                <StackPanel Margin="0,0,16,0">
                                    <TextBlock Text="Biển số xe" FontWeight="SemiBold" FontSize="12" Foreground="#434655" Margin="4,0,0,6"/>
                                    <ComboBox x:Name="txtBienSo" Height="42" Padding="12,10" Background="White" BorderBrush="#E1E2ED"/>
                                </StackPanel>

                                <StackPanel Margin="16,0,0,0">
                                    <TextBlock Text="Ngày sửa chữa" FontWeight="SemiBold" FontSize="12" Foreground="#434655" Margin="4,0,0,6"/>
                                    <DatePicker x:Name="dpNgayTiepNhan" Height="42" Padding="12,6" VerticalContentAlignment="Center" Background="White" BorderBrush="#E1E2ED"/>
                                </StackPanel>
                            </UniformGrid>
                        </StackPanel>
                    </Border>

                    <Border Background="White" BorderBrush="#E1E2ED" BorderThickness="1" CornerRadius="8" Padding="0" Margin="0,0,0,24">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Padding="24,16" BorderBrush="#E1E2ED" BorderThickness="0,0,0,1" Background="#FAF8FF">
                                <Grid>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <materialDesign:PackIcon Kind="FormatListBulleted" Foreground="#2563EB" Width="20" Height="20" VerticalAlignment="Center"/>
                                        <TextBlock Text="Danh sách hạng mục sửa chữa" FontSize="14" FontWeight="SemiBold" Foreground="#191B23" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <TextBlock Text="* Click vào dòng trống cuối cùng để thêm mới" FontSize="12" Foreground="#737686" HorizontalAlignment="Right" VerticalAlignment="Center" FontStyle="Italic"/>
                                </Grid>
                            </Border>

                            <DataGrid x:Name="dgvChiTiet" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="True"
                                      CellEditEnding="dgvChiTiet_CellEditEnding" Style="{StaticResource ModernDataGrid}"
                                      BorderThickness="0" Margin="0" MinHeight="250" Padding="10">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Nội dung" Binding="{Binding NoiDung}" Width="2*"/>
                                    <DataGridComboBoxColumn Header="Vật tư phụ tùng" x:Name="colVatTu"
                                                            SelectedValueBinding="{Binding VatTuPhuTungId}"
                                                            DisplayMemberPath="TenPhuTung" SelectedValuePath="Id" Width="2*"/>
                                    <DataGridComboBoxColumn Header="Loại tiền công" x:Name="colTienCong"
                                                            SelectedValueBinding="{Binding TienCongId}"
                                                            DisplayMemberPath="TenNoiDung" SelectedValuePath="Id" Width="2*"/>
                                    <DataGridTextColumn Header="Số lượng" Binding="{Binding SoLuong}" Width="*"/>
                                    <DataGridTextColumn Header="Đơn giá" Binding="{Binding DonGiaPhuTung}" IsReadOnly="True" Width="*"/>
                                    <DataGridTextColumn Header="Tiền công" Binding="{Binding ChiPhiTienCong}" IsReadOnly="True" Width="*"/>
                                    <DataGridTextColumn Header="Thành tiền" Binding="{Binding ThanhTien}" IsReadOnly="True" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>

                            <Border Grid.Row="2" Background="#EDEDF9" Padding="24,16" BorderBrush="#E1E2ED" BorderThickness="0,1,0,0">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <TextBlock Text="Tổng tiền thanh toán: " VerticalAlignment="Center" Foreground="#434655" FontWeight="Medium" FontSize="14"/>
                                    <TextBlock x:Name="txtTotalAmount" Text="0" VerticalAlignment="Center" FontWeight="Bold" FontSize="20" Foreground="#2563EB" Margin="8,0,4,0"/>
                                    <TextBlock Text="VNĐ" VerticalAlignment="Center" Foreground="#2563EB" FontSize="12" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>

                    <Grid Margin="0,12">
                        <Button HorizontalAlignment="Left" Content="In phiếu" Width="120" Height="42" Background="Transparent" Foreground="#545F73" BorderBrush="#737686" BorderThickness="1">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                        </Button>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="btnDong" Content="Hủy lệnh / Đóng" Width="140" Height="42" Margin="0,0,16,0" Background="Transparent" Foreground="#545F73" BorderThickness="0" Click="btnDong_Click"/>
                            <Button x:Name="btnLuu" Content="Lưu phiếu sửa chữa" Width="180" Height="42" Background="#2563EB" Foreground="White" FontWeight="Bold" BorderThickness="0" Click="btnLuu_Click">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="8"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </Grid>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
