<Window x:Class="WpfApp1.QuyDinhHistoryPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Toàn bộ lịch sử quy định"
        Height="700"
        Width="1100"
        WindowStartupLocation="CenterOwner"
        Background="#FAF8FF"
        FontFamily="Inter">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="TOÀN BỘ LỊCH SỬ CẬP NHẬT QUY ĐỊNH" FontSize="22" FontWeight="Bold" Foreground="#191B23"/>
        </StackPanel>

        <DataGrid x:Name="dgvAllHistory" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  Background="White" BorderBrush="#E1E2ED" BorderThickness="1" RowHeight="40">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Thời gian" Binding="{Binding ThoiGian}" Width="2*"/>
                <DataGridTextColumn Header="Quy định" Binding="{Binding QuyDinh}" Width="2*"/>
                <DataGridTextColumn Header="Giá trị cũ" Binding="{Binding GiaTriCu}" Width="*"/>
                <DataGridTextColumn Header="Giá trị mới" Binding="{Binding GiaTriMoi}" Width="*"/>
                <DataGridTextColumn Header="Người thực hiện" Binding="{Binding NguoiThucHien}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Đóng" Width="120" Height="40" Click="BtnClose_Click" Background="#2563EB" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>